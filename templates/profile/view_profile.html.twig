{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user %}

    <h1>Mon profil</h1>

    <div class="ProfilEdit">

        <div class="form-group">
            <br><br>
            <p>Bienvenue à toi {{ app.user.username }}</p>
            <br>
        </div>

        <a href="{{ path('edit_profile', {id: app.user.id}) }}">Editer profil</a>

    </div>

    <img class="logogauche" src="{{ asset('images/lebonsteam1.png') }}" alt="le logo du site" height="500">
    <img class="logodroite" src="{{ asset('images/lebonsteam2.png') }}" alt="le logo du site" height="500">

    {% endif %}
{% endblock %}
